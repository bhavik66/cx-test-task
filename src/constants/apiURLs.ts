import apiConfig from './apiConfig';

export const apiBaseURLLocalMock = '/api';
export const apiBaseURLSwaggerMock = 'https://localhost:4000';
export const apiBaseURL = apiConfig.BASE_URL;

export const apiEndPoints = {
  USERS: '/users',
  USER: '/user',
  LOGINS: '/logins',
  LOGIN: '/login',
  BANKS: '/banks',
  BANK: '/bank',
  REGISTERS: '/registers',
  REGISTER: '/register',
  // Add more endpoints here
};
